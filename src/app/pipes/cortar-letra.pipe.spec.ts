import { CortarLetraPipe } from './cortar-letra.pipe';

describe('CortarLetraPipe', () => {
  it('create an instance', () => {
    const pipe = new CortarLetraPipe();
    expect(pipe).toBeTruthy();
  });
});
