import { DatepipeDiamesPipe } from './datepipe-diames.pipe';

describe('DatepipeDiamesPipe', () => {
  it('create an instance', () => {
    const pipe = new DatepipeDiamesPipe();
    expect(pipe).toBeTruthy();
  });
});
