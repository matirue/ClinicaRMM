import { RoundDivDirective } from './round-div.directive';

describe('RoundDivDirective', () => {
  it('should create an instance', () => {
    const directive = new RoundDivDirective();
    expect(directive).toBeTruthy();
  });
});
